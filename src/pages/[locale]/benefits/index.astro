---
/**
 * Components
 */
import Layout from "@/layouts/Layout.astro";
import HeroBenefits from "@/components/Benefits/Hero.astro";
import ContentBenefits from "@/components/Benefits/ContentBenefits.astro";
import BenefitsPage from "@/components/Benefits/BenefitsPage";

/**
 * Utils
 */
import { getLangFromUrl, useTranslations, generateLocalizedPaths } from "@/i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
export async function getStaticPaths() {
  const paths = await generateLocalizedPaths();
  return paths;
}
---


<Layout>
  <!-- <<HeroBenefits
    imageUrl="/images/benefits/illustration-benefits.png"
    title={t("benefits.hero.title")}
    description={t("benefits.hero.description")}
  />
  <ContentBenefits/>> -->
  <BenefitsPage />
</Layout>
